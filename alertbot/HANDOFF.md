# HANDOFF — alertbot (종가베팅 브리핑 + 수급 대시보드)

마지막 갱신: **2026-09-07 저녁**

---

## 0. 현재 상태 — 전부 자동으로 돌고 있다

- **텔레그램 브리핑**: 평일 8슬롯 + 일요일 18:00 주말점검. 채널 3개 분리 운영:
  - 종가베팅(`ALERTBOT_CHAT_ID`) / 업비트 상장(`TELEGRAM_CHAT_ID`) — 공개 성격
  - **LOG(`LOG_CHAT_ID=-1003782608263`) — 개인 리마인드 전용.** 발송은 `remind.py "..."`.
- **대시보드**: http://165.22.108.193/closebet.html — 세션별 순매수 표(코스피·코스닥) +
  시간대별 누적 순매수 차트 3개 + 일자별 마감 수급. 15:45(잠정)·18:40(확정) 자동 갱신.
- **데이터 축적** (서버 `/opt/alertbot/data/`, 커밋 안 됨):
  - `intraday/` 시간대별 순매수 1.5분 해상도, 08-28부터 (네이버 보관 1~2주라 매일 저장)
  - `snapshots.jsonl` 슬롯 스냅샷 + 거래소별(by_ex) — 세션 차분(sessions.py)의 원천
  - `reactions.jsonl` 지표→자산 반응 실측 (158건 백필)
  - `predictions.jsonl` 미국→한국 예측 적중률 — **09-08 아침부터 기록 시작**
  - `sector_map.json` 종목→섹터 2,116종목 (일요일 주간 갱신)

## 1. 다음 세션 후보 작업 (우선순위 순)

1. **예측 적중률 첫 판독** — 1~2주 뒤 `python3 predictions.py` 로 드라이버→섹터
   조합별 방향/초과수익 적중률 확인 → `sectors.US_TO_KR` 매핑 정리.
2. **주도 테마를 08:50 프리마켓에도** — 테마 구성종목 캐시 필요(키움 ka90001/ka90002,
   약 200콜, sector_map 과 같은 주간 캐시 패턴으로).
3. **대시보드 고도화** — 세션별 추이(일자별 프리/정규/애프터 스택), 반응 로그·적중률 표 추가.
4. **16:30 알림의 선물 확정 이슈** — 선물 순매수 확정이 날마다 다름(목 ~17:35, 금 ~16:30).
   16:30 값이 잠정일 수 있음. 며칠 관찰 후 문제되면 "선물 잠정" 표기 or 슬롯 조정.
5. 키움 로컬 인증: 집 IP 변경 시 openapi.kiwoom.com 재등록 필요 (09-07 현재 61.73.154.67).

## 2. 이번 주 확정된 사실 (재조사 불필요)

- **재배분은 상시가 아니다**: 09-03 저녁(18:04→18:06)의 기타법인 +1.6조 → 기관/외국인
  재배분은 간헐적 정정. 09-04 는 주말이 지나도 원값 유지. **기타법인 ~+1.6조/일이 정상 상태**
  (자사주 매입). 18:40 재수집이 정정까지 포함한 최종 시계열을 남긴다.
- **네이버 선물 순매수** = `/api/index/FUT/trend`, 억원, 키움 0780 과 일치.
  `integration.dealTrendInfo` 는 장중 선물에서 잠정치 → 쓰지 말 것.
- **네이버 지수 API = KRX 전용** (키움 stex=1 과 원 단위 일치). NXT 는 키움 stex=2.
  선물은 NXT 에서 거래되지 않으므로 KRX = 전체.
- KRX·NXT **거래소 접수형 스톱지정가 주문 존재** (키움 [4991]). 자동감시주문 [0624] 는
  키움 서버 감시(HTS 꺼도 90일). 스톱은 단일가(동시호가·VI 중)에서 효력 없음.
- 시간대별 순매수 소스: `investorDealTrendTime.naver?bizdate&sosok&page`
  (01 코스피/02 코스닥/03 선물·계약단위, 1.5분 해상도, euc-kr).

## 3. 크론 (서버 UTC = KST-9. diff 확인 후 수정할 것 — upbit_bot 운영 잡 동거)

```
알림:   21:00(0600) 22:50(0750) 23:50(0850) 00:30(0930) 05:30(1430)
        07:30(1630) 10:00(1900) 11:00(2000) / 일 09:00(1800 --force)
수집:   06:35 collect.py 1535 (세션 경계, 알림 없음)
        06:45 intraday+dashboard (잠정) / 09:40 intraday+dashboard+predictions --evaluate (확정)
주간:   일 08:00 nxt.py --build-map (섹터 맵)
```

## 4. 슬롯별 변동폭 기준 (사용자 결정, 09-07 확정)

| 슬롯 | 창 | 코스피 소스 |
|---|---|---|
| 06:00/07:50/08:50 | 전일 15:30 → 지금 | EWY 프록시 |
| 09:30/14:30 | 전일 15:30 → 지금 | **실제 지수**(전일比) |
| **16:30** | **전일 15:30 → 16:30 (하루 전체)** | **실제 지수 종가 등락률** (`SLOTS prev_close` 플래그) |
| 19:00/20:00 | 당일 15:30 → 지금 | EWY 프록시 |

## 5. 메시지 구성 (사용자 결정 반영)

발표 완료(24h, 실제/예상+실측반응, 신규상장 공모가대비%) → 시황(오일·금·미국10Y·나스닥·
코스피·코스닥장중·비트코인) → 관련 뉴스(24h 상한) → 거래대금(5일 기준 표시)/순매수
(코스피+선물/코스닥, 단위 자동) → 주도 섹터(거래대금 중앙값 필터+대표주) → 주도 테마
(주도주 돈 순, 재료 중복 제거) → 주도주(섹터 태깅) → [0850: NXT 프리마켓 주도섹터/주도주]
→ [아침: 미국 섹터+한국 영향(예측 기록됨)] → 예정(★★★만: 지표48h/실적·상장·휴장 7일,
과거반응 통계, ⚠=오버나이트)

## 6. 함정 (반복해서 걸린 것들)

- **Bash 히어독이 `\\n`을 실제 개행으로 바꾼다** → 파이썬 소스 조작은 Write 툴 또는
  `chr(92)+"n"` 조립. 증상: `unterminated string literal`.
- `py -3.11` (PATH 의 python 은 pandas 없는 upbit_bot venv) / 서버 SSH `~/.ssh/do_key_home`
- cp949 콘솔 → 진단 출력은 ASCII 로
- 텔레그램 ReadTimeout 재시도 금지(중복 발송) — 대부분 실제로는 전송됨
- crontab 수정은 diff 미리보기 필수
- 백필류 루프에서 **주말을 '빈 날'로 세지 말 것** (월요일 아침에 조기 중단됨)
- deploy.sh 가 올리는 것: `*.py *.json *.html README.md run.sh` — 새 확장자는 추가 필요
